import
	keen/math/math
	keen/math/vec

+line32 record
	angle float32
	distance float32

+project vec2-32(a line32, pos vec2-32)
	cos = cos a.angle
	sin = sin a.angle
	normal vec2-32 = cos, sin
	x: dot pos, normal.rotate-right
	y: dot pos, normal
