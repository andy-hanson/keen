import
	keen/col/queue

test
	a nat64 queue = ()
	assert is-empty a
	a.to is ()
	forbid _ = dequeue a

	b = a ~ 1
	b.to::nat64[] is (1,)
	bx, br = dequeue! b
	bx is 1
	assert is-empty br

	c = b ~ 2
	c.to::nat64[] is (1, 2)
	cx, cr = dequeue! c
	cx is 1

	cr.to::nat64[] is (2,)
	crx, crr = dequeue! cr
	crx is 2
	assert is-empty crr

	d = c ~ 3
	d.to::nat64[] is (1, 2, 3)

	map::nat64[] d, it + 10 is (11, 12, 13)
