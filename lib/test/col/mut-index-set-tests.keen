import
	keen/col/mut-index-set

test
	a mut-index-set = 10, 100, 1000
	is-empty a is false
	size a is 3
	assert a[10]
	assert a[100]
	assert a[1000]
	forbid a[7]
	to::nat[] a is 10, 100, 1000

	a -= 100
	size a is 2
	to::nat[] a is 10, 1000

	a ~= 1050
	assert a[1050]
