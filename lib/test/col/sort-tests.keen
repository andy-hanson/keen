test
	()::nat64[].sort is ()
	(3, 1, 2)::nat64[].sort is (1, 2, 3)

	# Test that it's a stable sort
	arr char8[] buffer = "2a", "2b", "1a", "1b"
	arr sort= it[0]
	arr.to::char8[][] is ("1a", "1b", "2a", "2b")
	arr sort= it[1]
	arr.to::char8[][] is ("1a", "2a", "1b", "2b")
