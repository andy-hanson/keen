# TODO: I moved most of this out, no need for this to be its own module now ---------------------------------------------------------
import
	crow/crypto/pseudo-random
	../../crow/col/experimental/object-memoizer
	../model/model

all-insts record mut
	-prng prng
new all-insts()
	(),

next-hash nat32(a all-insts)
	a.prng next

get-all-mut-array-impls struct-inst[struct-inst](
	mut-array struct-decl,
	cb-make-mut-array-impl struct-inst mut(args type-args),
)
	keys struct-inst[] = with out : build
		trusted for x : mut-array.insts!
			out ~= x
	for x : keys
		x, cb-make-mut-array-impl[x type-args]
