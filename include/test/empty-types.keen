test
	a r = ()
	a is a
	a <=> a is equal
	a.to::json is empty-object

	state hash-state = ()
	hash a, state
	finish state is finish ()::hash-state

test
	xs r[] = (),
	xs.size is 1

-r record
- == bool(a r, b r)
- <=> comparison(a r, b r)
- hash void(a r, state hash-state)
-to json(a r)
