# The JVM doesn't like anything happening after a 'throw',
# so we have to call 'Keen.throw_' instead in situations like this.
test
	try
		foo
	catch e as error
		show e is "boo"

foo void()
	throw error "boo"
	log "This will not appear"
