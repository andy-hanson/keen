import
	crow/col/shared-array
	crow/parallel

# TODO: can't this go in shared-array.crow? ----------------------------------------------------------------------------------
test
	guard !(trusted extern java) # TODO -----------------------------------------------------------------------------------------------
	guard !(trusted extern js) # TODO -------------------------------------------------------------------------------------------------
	res nat shared[] = ()
	for x : 0::nat .. 100 parallel
		res ~= x
		()
	res.move-to::nat[].sort is (0 .. 100).to
	()
