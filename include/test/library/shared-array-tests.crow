import
	crow/col/shared-array
	crow/parallel

# TODO: can't this go in shared-array.crow? ----------------------------------------------------------------------------------
test
	do unless trusted java # TODO ----------------------------------------------------------------------------------------------------------
	do unless trusted js # TODO ------------------------------------------------------------------------------------------------------------
	res nat shared[] = ()
	for x in parallel 0::nat .. 100
		res ~= x
		()
	res.move-to::nat[].sort is (0 .. 100).to
	()
