test
	xs nat[] = 3, 1, 2
	xs.min-and-max is (1, 3)
	xs.min is 1
	xs.max is 3
	try
		_ = ()::nat[] min
		should-throw
	catch empty-collection _
		()

test
	xs nat[] = 53, 61, 72
	xs min-and-max-by (x => x % 10) is do
		elems: (61, 53)
		keys: (1, 3)

-to[t] json(a t min-and-max) (json, t) to
-to[elem, key] json(a (elem, key) min-and-max-by) (json, elem) to, (json, key) to
