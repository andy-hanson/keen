test
	the r = 1, 2
	the::r.x is 1
	foo 10 is 12
	?foo () is ()
	?foo 20::nat? is 22
	(foo: 42)::j.x is 1

-r record(x nat, y nat)

-foo nat(a nat) r
	a + bar

-bar nat() r
	the::r.y

-j record(x nat)
-named-new j(_ symbol[], _ nat[]) r
	the::r.x,

test
	the r = 1, 2
	x nat mut() = of
		the::r.x
	x[] is 1

test
	the nat = 1
	foo is 1
s1 spec nat
s2 spec s1
foo nat() s2
	the::nat
