import
	keen/unsafe
	java/lang/Iterable
	java/util/AbstractList
	java/util/ArrayList
	java/util/Iterator
	java/util/List

test java
	do trusted
	a ArrayList = ()
	# ArrayList extends AbstractList
	b AbstractList = a
	add b, a is true
	# ArrayList implements List
	c List = a
	c.size is 1
	# ArrayList extends AbstractList implements Iterable
	i Iterable = a
	assert reference-equal i.iterator.next, a
