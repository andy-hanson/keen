use-prefix
no-std
import
	./compare: comparison
	./private/bootstrap: nat32, nat64
export
	./private/bootstrap: bool, false, true

+new bool()
	false

+== bool(a bool, b bool)
| Compares {false} less than {true}.
+<=> comparison(a bool, b bool)

+to nat32(a bool)
+to nat64(a bool)
