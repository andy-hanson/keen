no-std
import
	./compare: comparison
	./private/bootstrap: nat8
export
	./private/bootstrap: bool, false, true

+new bool()
	false

+== bool(a bool, b bool)
| Compares {false} less than {true}.
+<=> comparison(a bool, b bool)

+to nat8(a bool)
