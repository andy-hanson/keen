+prng record mut
	-state mut nat64
+new prng()
	0xdecafc0ffeecafe,
+next nat64(a prng)
	# Uses the value from musl
	a.state := 0x5851f42d4c957f2d wrap-mul a.state wrap-add 1
	a state
+next nat32(a prng)
	a.next::nat64 wrap-to
