test
	x mut nat = 0
	y nat = until
		if x == 5
			x * 2
		else
			x +:= 1
			()
	x is 5
	y is 10

	until x == 15
		x +:= 1
	x is 15

	while x > 10
		x -:= 1
	x is 10

test
	xs nat mut[] = 1, 2, 3
	result nat?[] = with out in build
		while
			x = pop xs
			out ~= x
			x
	result is 3, 2, 1, ()
