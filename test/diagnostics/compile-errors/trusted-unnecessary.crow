import
	crow/unsafe

f nat() unsafe
	trusted gc-safe-value

f nat()
	trusted trusted gc-safe-value

f nat()
	# testing twice to verify that we reset 'usedTrusted' after each 'trusted'
	_ nat = trusted gc-safe-value
	trusted gc-safe-value
