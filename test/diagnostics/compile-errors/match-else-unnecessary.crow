main void()
	log x.match-e
	log r.match-u

e enum(x, y)

match-e string(a e)
	match a
	as x
		"x"
	as y
		"y"
	else
		"other"

u union(r, s)
r record
s record

match-u string(a u)
	match a
	as r
		"r"
	as s
		"s"
	else
		"other"
