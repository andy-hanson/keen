main void()
	()

f void() bare, bare

nat64 builtin data

en enum(x) data

f flags data

r record data

u union(nat) data

dd data, data

mm record mut, mut

ss record shared, shared

data spec builtin, builtin
