test
	the m = 1,
	the s = 2,
	_ nat mut() = of
		the::m.x + the::s.x
	_ nat shared() = of
		the::m.x + the::s.x
	_ nat data() = of
		the::m.x + the::s.x

test
	_ void mut() = of
		the m = 1,
		_ void mut() = of
			the::r.x

m record(x nat) mut
s record(x nat) shared
