main void()
	()

f void() bare, bare, libc extern, native extern

nat64 builtin data

e extern mut

ee extern extern

en enum(x) data

f flags data

r record data

u union(nat) data

uv union(nat) by-val

dd extern data, data

mm record mut, mut

ss record shared, shared

rr record by-ref, by-ref

vv record by-val, by-val

g global(nat) libc extern, native extern

data spec builtin, builtin
